#!/bin/python3

import math
import os
import random
import re
import sys

# Complete the reverseArray function below.
def reverseArray(a):
    a.reverse()
    return a

print(reverseArray([1, 4, 3, 2]))
# Note that the code to format the input/output takes an array as the
# output of this function and then formats it - I was formatting it in the
# function and it said I was wrong...
